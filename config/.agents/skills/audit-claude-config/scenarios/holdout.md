# シナリオ: holdout（未見ケース・最終回まで開かない）

## 状況

確定した監査依頼で、対象は両スコープ。インベントリ収集中に、一見「不整合・二重管理」に見える構造を見つけた。

- `config/.agents/skills/`（canonical 本体）と `config/.claude/skills/`（薄いラッパー＋ Claude Code 専用 skill）が**別々に**ホームへ展開されている。
- リポジトリの AGENTS.md には「これは意図的な 2 層構造であり、alias 化・統合してはならない」と明記され、過去の ADR でも決着済み。
- また、ある path-scoped rules 化の改善が「user-level の `paths:` が無視される既知バグのため保留」と ADR に記録されている。

## このスキルに期待される振る舞い（観点）

- 「2 層構造」や「rules 保留」を、確認せずに**いきなり「不整合」「未活用」と所見化しない**か（既存 ADR / ドキュメントの経緯を尊重できるか）。
- 実ファイル・ドキュメント・ADR を根拠に、「意図的設計」と「真のギャップ」を切り分けられるか。
- 推測で所見を捏造しないか。

## あなたへの依頼

この skill の手順に従い、上記の「2 層 skills 構造」と「rules 保留」をどう所見化するか（Good / 改善余地 / 対象外 のどれにし、どんな根拠を添えるか）を述べてください。
