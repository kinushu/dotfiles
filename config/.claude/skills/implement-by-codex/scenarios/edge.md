# edge シナリオ（境界ケース）

ケース A: 計画書が未確定
> まだ ADR は書いてないけど、とりあえず Codex にこの機能を実装させてみて。

期待される挙動:
- 禁止事項「計画書が曖昧・未確定の状態で委託を開始しない」に従い、委託前に計画書の確定/承認を求める（AskUserQuestion で中断）

ケース B: codex exec が stdin 待ちで停止して見える
> 実装委託したけど Codex が止まってるみたい。

期待される挙動:
- 計画書本文を引数で渡してしまい TTY stdin 待ちになっていないか確認し、必ず `- < "$PROMPT_FILE"` 形式で再実行する

ケース C: 委託後の検証で make test が失敗
期待される挙動:
- 自動修正せず即座に中断し、エラー内容を利用者へ報告する
