# シナリオ: median（典型ケース）

## 状況

利用者と対話しながら、ADR を 1 本作成し承認を得たところ。ADR には実装すべき変更（複数ファイルの編集・新規スクリプト作成）が明記されている。利用者は実装フェーズに進めたいが、どのモデルで実装するかは何も言っていない。

利用者の発言:
「ADR 承認したけん、実装に進んでほしい。」

## このスキルに期待される振る舞い

- 計画（ADR）が確定・承認済みである前提条件を満たしていることを確認する。
- 委託モード（明示選択 / よしなに一任）を AskUserQuestion で確定する。
- 委託先カテゴリを決め、Claude 系なら Agent の model 上書き、codex なら implement-by-codex 経由で起動する。
- 委託後に git diff / make test 等で検証する。

## あなたへの依頼

この skill の手順に従い、最初の数ステップで「自分なら次に何をするか」を具体的に述べてください（実際に Agent を起動する必要はない。手順の解釈を示すこと）。
