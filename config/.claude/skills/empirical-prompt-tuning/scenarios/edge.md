# scenario: edge

**役割**: 境界ケース・例外ケース。median では表面化しない指示の穴を炙り出す。

**書き方の方針**:

- median の入力から 1 要素だけ欠けさせる／壊す
- 外部ツールが失敗する前提を置く（例: `codex` コマンドが未インストール）
- 引数が曖昧・矛盾する前提を置く
- 複数シナリオを書いても良い（`edge-1.md`, `edge-2.md` と分ける）

---

## 入力（ケース A: 前提欠落）

<!-- 例: commit-changes skill なら、git status が空（変更なし）のケース -->

### 期待する挙動

<!-- 例:
- skill が AskUserQuestion で「commit 対象がない」ことを確認してから終了する
- または明示的なエラーメッセージで中断する
-->

## 入力（ケース B: 外部依存失敗）

<!-- 例: review-by-codex skill なら、codex CLI が未インストール／認証切れ -->

### 期待する挙動

<!-- 例:
- skill 冒頭で `codex --version` の疎通チェックを行い、失敗したら中断する
- 環境導線（install 手順など）を案内する
-->

## 入力（ケース C: 引数曖昧）

<!-- 例: research-topic skill で期間指定が「最近」のように曖昧 -->

### 期待する挙動

<!-- 例:
- AskUserQuestion で期間を選ばせる（今週 / 今月 / 指定日付範囲）
-->

## Output 採点項目

- [ ] 想定外入力に対して skill が暴走しない（勝手に補完しない）
- [ ] 中断時のメッセージが利用者にとって理解可能
- [ ] 再実行できる状態で終わる（半端な副作用を残さない）

## Process 採点項目

- AskUserQuestion が適切なタイミングで呼ばれているか
- 不要な探索（無関係なファイルの Read 等）が発生していないか
